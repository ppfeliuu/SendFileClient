import {
  MOSTRAR_ALERTA,
  LIMPIAR_ALERTA,
  SUBIR_ARCHIVO_OK,
  SUBIR_ARCHIVO_ERROR,
  CREAR_ENLACE_OK,
  CREAR_ENLACE_ERROR,
} from "../../types";

export default (state, action) => {
  switch (action.type) {
    default:
      break;
  }
};
